let fs;_6e6‍.x([["createPrismaSchema",()=>createPrismaSchema]]);_6e6‍.w("fs",[["default",["fs"],function(v){fs=v}]]);

       const createPrismaSchema = ({ base, template }) => {
  fs.writeFileSync(
    `${base}/prisma/schema.prisma`,
    `datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["referentialActions"]
}  
`
  );
};
